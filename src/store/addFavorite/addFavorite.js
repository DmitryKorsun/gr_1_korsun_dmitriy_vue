/*
export default {
    state:{
        list: list ? this.$store.state.FilmsNew.moviesNew: []
    },
    mutations:{
        add_to_favorites_list(state, mov){
            let movieDatabase = state.list.find(current => current.idMovies ==mov.idMovies)
            if (!movieDatabase){
                this.state.list.push(mov)
            }
        }
    }
}*/
